# Copy to android/key.properties and fill in. That file is gitignored - never commit it.
# storeFile must be an absolute path to the keystore, which lives OUTSIDE this repo.
storeFile=/Users/YOUR_USER/.keystores/hanzipractice-upload.jks
storePassword=
keyAlias=upload
keyPassword=
